# Pillow is required: it powers the exact 1080x1350 center-crop in
# openai_render.py and the headline compositing in compose_text.py.
# Everything else uses the Python standard library (urllib, base64).
Pillow>=10.0
