# Pillow: exact 1080x1350 crop (openai_render.py) + headline compositing
# (compose_text.py).
# fontTools: instances variable Google Fonts to a static weight in get_font.py.
# Everything else uses the Python standard library (urllib, base64).
Pillow>=10.0
fonttools>=4.40
